buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

ext {
    sourceCompatibility = JavaVersion.VERSION_1_8
}

allprojects {
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
